# Job 层

定时任务服务目录。

## 用途

用于存放定时任务、批处理作业等周期性执行的代码。

## 结构示例

```
job/
├── sync_data/       # 数据同步任务
├── statistics/      # 统计任务
├── cleanup/         # 清理任务
└── ...
```

## 注意事项

- 按任务类型组织
- 可使用 cron 表达式调度
- 复用 `model/` 和 `pkg/` 中的代码
